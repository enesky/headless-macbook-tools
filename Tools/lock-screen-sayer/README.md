# Lock Screen Voice Alert

A LaunchAgent managed by Halftop. It says `Lock Screen` when the macOS session becomes locked.

Runtime files are installed under:

```text
~/Library/Application Support/Halftop/Agents/lock-screen-sayer
```

Normal installation and removal should be performed with the Lock Screen Voice Alert switch in Halftop.
